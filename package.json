{
  "devDependencies": {
    "jest": "^26.0.1",
    "prettier": "^2.0.5",
    "static-server": "^2.2.1"
  },
  "scripts": {
    "lint": "prettier -c *.js",
    "lint:fix": "prettier --write *.js",
    "start": "cd imperial-vue && yarn serve",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  }
}
